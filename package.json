{
  "name": "svg-sprite-generator",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "optimize": "svgo -f svg/raw -o svg/opt --disable=mergePaths --disable=convertPathData",
    "generate": "spritesh --input svg/opt --output sprite.svg"
  },
  "author": "fc",
  "license": "ISC",
  "dependencies": {
    "spritesh": "^1.2.1",
    "svgo": "^1.3.2"
  }
}